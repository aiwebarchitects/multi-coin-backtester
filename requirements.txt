# Multi-Crypto Multi-Algorithm Backtester Requirements

# Core data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# HTTP requests for API calls
requests>=2.28.0

# Type hints support (included in Python 3.5+, but explicit for clarity)
typing-extensions>=4.0.0

# Optional: For enhanced data visualization (if plotting is enabled)
matplotlib>=3.6.0

# Optional: For better performance with large datasets
numba>=0.56.0
